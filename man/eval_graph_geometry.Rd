% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_game_tracker.R
\name{eval_graph_geometry}
\alias{eval_graph_geometry}
\title{Geometry for the evaluation graph}
\usage{
eval_graph_geometry(cp, width = 420, height = 110)
}
\arguments{
\item{cp}{Centipawn evaluations from White's point of view, one per position
(so one more than the number of moves). \code{NA} where not yet measured.}

\item{width, height}{Pixel size of the graph.}
}
\value{
A list with \code{n} points, \code{xs}/\code{ys} coordinates, \code{points} and \code{area}
as SVG coordinate strings, \code{mid} (the halfway line) and \code{hit_width} (the
width of one clickable band); or \code{NULL} if there is nothing to draw.
}
\description{
The arithmetic behind the graph, with no drawing: where each point sits and
what shapes join them. Rendering that into a picture is the caller's job.
They are
split so this layer stays free of any UI toolkit - see the note in
\code{test-core-boundary.R}.
}
\details{
Evaluations are squashed with the same logistic the eval bar uses, so a
three-pawn edge and a nine-pawn one stay visibly different without the graph
being dominated by a single decisive moment.

Positions that have not been evaluated yet carry the last known value
forward rather than breaking the line, so the graph does not flicker while
the engine catches up with a fast game.
}
