% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_human_model.R
\name{human_model_available}
\alias{human_model_available}
\title{Is a human model usable right now?}
\usage{
human_model_available(rating = 1500L)
}
\arguments{
\item{rating}{Desired rating.}
}
\value{
\code{TRUE} when lc0 and at least one Maia network are present.
}
\description{
Asks whether the radar can say \emph{something} about a player of this strength,
not whether that exact network is installed - \code{\link[=maia_session_start]{maia_session_start()}} falls
back to the nearest one it has. Deliberately permissive, because it gates a
feature: refusing to model a 1300 because only 1100 and 1500 are downloaded
would turn the radar off for a rating it can very nearly answer.
}
\details{
Build-time checks that a specific set of weights really is present should
test the files directly rather than call this.
}
