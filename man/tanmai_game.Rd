% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{tanmai_game}
\alias{tanmai_game}
\title{A chess game}
\description{
A data frame with \strong{one row per ply}, returned by \code{\link[=read_pgn]{read_pgn()}} and
\code{\link[=read_video]{read_video()}}. This is the shape the whole package is built around: every
analysis reads it and none of them ask which adapter produced it, which is
why a new way of getting a game in costs almost nothing.
}
\details{
It is an ordinary data frame, so accuracy by player is a \code{summarise()}, the
evaluation graph is a \code{ggplot()}, and the moves that decided the game are a
\code{slice_max()}.
}
\section{Columns}{

\describe{
\item{ply}{Half-move number, from 1.}
\item{move_no}{Full move number, as printed in a PGN.}
\item{side}{Who moved, \code{"w"} or \code{"b"}. Derived from the position, not
assumed, so a game starting from a set-up position is right.}
\item{san, uci}{The move played, in both notations.}
\item{fen_before, fen_after}{The positions either side of the move. Each
row's \code{fen_after} is the next row's \code{fen_before}.}
\item{cp}{Evaluation after the move, in centipawns, always from \strong{White's}
point of view so the sign means the same thing down the whole column.
\code{NA} until \code{\link[=evaluate]{evaluate()}} is run.}
\item{cp_loss}{What the move cost, from the \strong{mover's} point of view -
the only reading under which "you lost three pawns" makes sense for both
colours. Never negative.}
\item{best_uci, best_san}{What the engine would have played instead.}
\item{class}{A factor: best, good, inaccuracy, mistake or blunder.}
\item{human_p}{Probability a human of a given rating plays this move.
Reserved; not yet filled in.}
}
}

\section{Attributes}{

\code{headers} carries a PGN's header tags. \code{ledger} (from \code{\link[=read_video]{read_video()}}) records
what became of every frame that was looked at.
}

\seealso{
\link{tanmai_position}
}
