# Locate the directory holding a set's 12 SVGs

Locate the directory holding a set's 12 SVGs

## Usage

``` r
piece_set_path(name)
```

## Arguments

- name:

  A piece-set name (or "cburnett" for the bundled default).

## Value

The directory path, or `NULL` if the set isn't available locally.
