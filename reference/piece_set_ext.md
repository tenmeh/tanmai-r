# Find which file extension a downloaded piece set uses

Find which file extension a downloaded piece set uses

## Usage

``` r
piece_set_ext(dir)
```

## Arguments

- dir:

  A piece-set directory.

## Value

The extension for which all 12 piece files are present, or `NULL`.
