# Fetch and build templates for every set in the manifest

Fetch and build templates for every set in the manifest

## Usage

``` r
setup_piece_sets(progress = NULL)
```

## Arguments

- progress:

  Optional callback `function(name, i, n)` for UI feedback.

## Value

A named logical vector (`TRUE` = set ready for auto-detection).
