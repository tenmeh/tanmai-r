# The piece-placement field of a FEN

The piece-placement field of a FEN

## Usage

``` r
fen_placement(fen)
```

## Arguments

- fen:

  A FEN string.

## Value

The placement field (everything before the first space), or `NA` for an
empty/missing input.
