# Path to a Maia weights file for a rating

Path to a Maia weights file for a rating

## Usage

``` r
maia_weights_path(rating = 1500L)
```

## Arguments

- rating:

  A rating with a Maia network: 1100 to 1900 in hundreds.

## Value

The expected weights path (which may not exist yet).
