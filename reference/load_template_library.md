# Load a template library from an RDS file

Load a template library from an RDS file

## Usage

``` r
load_template_library(path)
```

## Arguments

- path:

  Path to a saved template library.

## Value

A list with `pieces`, `norm_pieces` (normalized templates) and
`empty_threshold`.
